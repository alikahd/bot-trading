# 🔒 دليل الأمان - نظام Logging

## ⚠️ **تحذير أمني مهم**

**لا تستخدم `console.log` مباشرة في الكود!**

استخدام `console.log` في الإنتاج يعرض المشروع لثغرات أمنية:
- ❌ تسريب معلومات حساسة (API keys, tokens, user data)
- ❌ كشف بنية الكود والمنطق الداخلي
- ❌ تسهيل الهجمات والاختراقات
- ❌ مشاكل في الأداء

---

## ✅ **الحل الآمن: استخدام Logger**

### **الاستخدام الصحيح:**

```typescript
// ❌ خطأ - لا تفعل هذا
console.log('User data:', userData);
console.log('API Key:', apiKey);

// ✅ صحيح - استخدم logger
import logger from './utils/logger';

logger.log('User logged in');
logger.info('Fetching data...');
logger.warn('Low balance warning');
logger.error('Failed to load data');
logger.critical('Database connection lost'); // للأخطاء الحرجة فقط
```

---

## 🎯 **كيف يعمل Logger؟**

### **في بيئة التطوير (Development):**
```bash
npm run dev
# أو
npm start
```
- ✅ جميع الـ logs تظهر في Console
- ✅ مفيد للتطوير والتصحيح
- ✅ يمكن رؤية جميع الرسائل

### **في بيئة الإنتاج (Production):**
```bash
npm run build
```
- ✅ جميع الـ logs **مخفية تلقائياً**
- ✅ فقط الأخطاء الحرجة تظهر
- ✅ حماية كاملة من تسريب المعلومات

---

## 📋 **أنواع Logs المتاحة:**

| الدالة | الاستخدام | تظهر في الإنتاج؟ |
|--------|-----------|------------------|
| `logger.log()` | رسائل عامة | ❌ لا |
| `logger.info()` | معلومات | ❌ لا |
| `logger.warn()` | تحذيرات | ❌ لا |
| `logger.error()` | أخطاء | ❌ لا |
| `logger.debug()` | تصحيح | ❌ لا |
| `logger.critical()` | أخطاء حرجة | ✅ نعم |

---

## 🛡️ **قواعد الأمان:**

### ✅ **افعل:**
- استخدم `logger` بدلاً من `console`
- استخدم `logger.critical()` للأخطاء الحرجة فقط
- اختبر في بيئة الإنتاج قبل النشر
- راجع الكود للتأكد من عدم وجود `console.log`

### ❌ **لا تفعل:**
- لا تطبع معلومات حساسة (passwords, tokens, API keys)
- لا تطبع بيانات المستخدمين الشخصية
- لا تطبع تفاصيل الأخطاء الداخلية
- لا تستخدم `console.log` مباشرة

---

## 🔍 **التحقق من الأمان:**

### **البحث عن console.log في المشروع:**
```bash
# PowerShell
Get-ChildItem -Recurse -Include *.ts,*.tsx | Select-String "console\.(log|info|warn|error|debug)" | Where-Object { $_.Line -notmatch "logger" }

# أو استخدم البحث في VS Code
# ابحث عن: console\.(log|info|warn|error|debug)
# استثني: logger, originalLog, originalWarn, originalError
```

---

## 📦 **ملفات مستثناة:**

الملفات التالية **مسموح** لها باستخدام console:

1. **`src/utils/logger.ts`** - نظام Logger نفسه
2. **`src/components/data/RealDataToggle.tsx`** - Logger Viewer (محمي ببيئة التطوير)

---

## 🚀 **قبل النشر:**

✅ **Checklist:**
- [ ] تأكد من استخدام `logger` بدلاً من `console`
- [ ] اختبر في بيئة الإنتاج (`npm run build`)
- [ ] تحقق من عدم ظهور logs في Console
- [ ] راجع الكود للتأكد من الأمان

---

## 📞 **للمساعدة:**

إذا وجدت `console.log` في الكود:
1. استبدلها بـ `logger.log()`
2. أضف `import logger from './utils/logger'`
3. اختبر التغييرات

**المشروع الآن محمي من تسريب المعلومات!** 🔒✨
