[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python3 iqoption_unofficial_server.py"
healthcheckPath = "/api/status"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"

[env]
PORT = "5000"
PYTHONPATH = "/app"
