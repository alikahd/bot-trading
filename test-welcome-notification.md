# اختبار الإشعار الترحيبي

## الخطوات للاختبار:

### 1. افتح Console في المتصفح (F12)

### 2. قم بتشغيل هذا الكود لإرسال إشعار ترحيبي تجريبي:

```javascript
// استيراد الخدمات
import { adminNotificationService } from './services/adminNotificationService';
import { supabase } from './config/supabaseClient';

// جلب معلومات المستخدم الحالي
const { data: { user } } = await supabase.auth.getUser();
console.log('👤 المستخدم الحالي (auth):', user.id);

// جلب users.id
const { data: userData } = await supabase
  .from('users')
  .select('id, email, full_name')
  .eq('auth_id', user.id)
  .single();

console.log('👤 بيانات المستخدم:', userData);

// إرسال إشعار ترحيبي
const result = await adminNotificationService.sendWelcomeNotification(userData.id, false);
console.log('📧 نتيجة الإرسال:', result);
```

### 3. تحقق من Console:

يجب أن ترى:
```
📧 إرسال إشعار ترحيبي للمستخدم: [user-id]
✅ تم إرسال التنبيه الترحيبي (اشتراك جديد) للمستخدم: [user-id]
📧 بيانات الإشعار المُرسل: { id: "...", recipient_id: "...", title_ar: "مرحباً بك في BooTrading المميز!", type: "success" }
```

### 4. تحقق من نافذة الإشعارات:

- افتح نافذة الإشعارات (زر 🔔 في الهيدر)
- يجب أن ترى الإشعار الترحيبي

### 5. تحقق من البانر:

- يجب أن يظهر البانر في أعلى الصفحة مع الإشعار الترحيبي

## Console Logs المتوقعة:

### عند تحميل الصفحة:
```
👤 جلب تنبيهات للمستخدم auth_id: [auth-id]
✅ users.id: [user-id]
🔍 جلب التنبيهات الشخصية...
📥 التنبيهات الشخصية: 1
✅ تم جلب 1 تنبيه من قاعدة البيانات
✅ الاشتراك في الإشعارات لـ users.id: [user-id]
📡 حالة الاشتراك: SUBSCRIBED
```

### عند وصول إشعار جديد:
```
🔔 تنبيه شخصي جديد: { id: "...", title_ar: "مرحباً بك في BooTrading المميز!" }
🔔 تنبيه وصل فوراً!
➕ إضافة إشعار جديد
🔔 تنبيه جديد في البانر - عرض فوري!
```

## إذا لم يظهر الإشعار:

### تحقق من:

1. **قاعدة البيانات:**
```sql
SELECT * FROM notifications 
WHERE recipient_id = '[your-user-id]'
ORDER BY created_at DESC
LIMIT 5;
```

2. **سياسات RLS:**
```sql
SELECT * FROM pg_policies 
WHERE tablename = 'notifications';
```

3. **Console Errors:**
ابحث عن أي أخطاء في Console

4. **Realtime Connection:**
تحقق من أن Realtime متصل:
```
📡 حالة الاشتراك: SUBSCRIBED
```
